interface:
  display_name: "Plan + Goal"
  short_description: "One plan with checklist, clipboard copy, paste-ready goal prompt"
  default_prompt: "Write one implementation plan with a sequential checklist, copy it to the clipboard, then give a paste-ready prompt to execute that goal."
