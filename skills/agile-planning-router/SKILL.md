---
name: agile-planning-router
description: Orchestrates decision between simple plan, story, or epic. Use when you need to decide which planning artifact is appropriate for a demand, or when you need guidance on the planning flow.
compatibility: opencode
metadata:
  audience: engineering
  workflow: scrum-light
---

# Scrum Planning (Orchestrator)

Use this skill to decide which planning artifact is appropriate and direct to the correct skill.

Initial context received via slash: $ARGUMENTS

## Objective

- Evaluate the size and clarity of the demand
- Direct to the correct planning skill: `/plan`, `/story`, or `/epic`
- Ensure the chosen artifact is proportional to the delivery size

This skill complements the global rules in `~/.agents/rules/methodology.md`, `~/.agents/rules/workflow.md`, and `~/.agents/rules/plans.md`.

## Decision tree

### Use `/plan` when
- The change is localized (XS or S)
- There are few files
- The delivery fits in a single implementation cycle

### Use `/story` when
- There is a vertical delivery with several files (M)
- There is a richer acceptance criteria
- Validation requires stronger tests and review

### Use `/epic` when
- There are several dependent deliveries (L or XL)
- The initiative needs a roadmap
- Different stories can be planned and executed separately

### Still not clear?
- If the problem is not defined → use `/intake`
- If there is ambiguity about scope → use `/refinement`
- If you need strategic direction → use `/roadmap`

## Light sizing

| Size | Description | Artifact | Skill |
|---|---|---|---|
| XS | Localized adjustment, 1 file, low risk | Simple plan | `/plan` |
| S | Small delivery, few files, simple validation | Simple plan | `/plan` |
| M | Vertical delivery, several files, moderate validation | Story | `/story` |
| L | Large story, needs to be broken down | Epic | `/epic` |
| XL | Multi-story initiative, coordination needed | Epic | `/epic` |

## Mandatory rules

Every artifact generated by skills must contain:
1. **Context** (problem, objective, constraints)
2. **Files** (exact paths, action)
3. **Detail** (AS-IS, TO-BE, scope)
4. **Tasks** (verifiable checklist)
5. **Verification** (commands, validations, acceptance)

## Process

1. Evaluate the size of the demand with light sizing.
2. If the problem is not clear, suggest `/intake` or `/refinement`.
3. Direct to the correct skill: `/plan`, `/story`, or `/epic`.
4. Confirm with the user before proceeding.

## Location convention

- Simple plans: `.agents/plans/<name>.md`
- Stories and epics of initiatives: `planning/<initiative>/`
- Standalone stories: `.agents/plans/<name>.md`

## Relationship with the flow

```
/intake → /scrum-planning → /plan or /story or /epic
```

This skill is a router. It evaluates and directs, but does not produce the final artifact. To produce artifacts, use `/plan`, `/story`, or `/epic`.
